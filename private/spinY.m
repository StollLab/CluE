% ========================================================================
% Sy matrix
% ========================================================================
function Sy = spinY(spin)
Sy = (spinRaise(spin) - spinLower(spin))/2i;
end
