% ========================================================================
% Sx matrix
% ========================================================================
function Sx = spinX(spin)
Sx = (spinRaise(spin) + spinLower(spin))/2;
end

